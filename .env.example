# Server-side Anthropic key for the "Generate via Claude" feature.
# Used only by the backend (api/curate-seed.js and the Vite dev middleware).
# It is NEVER exposed to the browser. Copy this file to `.env` and fill it in.
ANTHROPIC_API_KEY=sk-ant-your-key-here
